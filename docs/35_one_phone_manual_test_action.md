> Historical development record: manual pairing UI has been removed. Current onboarding is QR-only; see `36_qr_pairing_v1.md` and `37_manual_pairing_cleanup.md`. Retained steps below are not current worker instructions.

# One-phone manual synthetic test action

Preparation, 2026-09-17. Phone upload has not yet occurred.

The specially built test APK enables private USB export and a manual first-batch send action. Ordinary builds disable both Dart UI and native export. Enable both with `OUTREACH_SYNTHETIC_SYNC_TEST=true` Dart define and Gradle project property `outreachSyntheticSyncTest=true`. These are test-build switches, not a production sync release.

Build verification caught that the Flutter command did not forward the Gradle environment property: initial installed artifact's native export was disabled. Corrective build uses explicit `-PoutreachSyntheticSyncTest=true` plus base64 `-Pdart-defines` with the Dart define, then verifies generated BuildConfig before installation. The initial artifact hash below is historical until correction is recorded. No export/upload occurred under that initial artifact.

Correction built successfully: generated native `SYNTHETIC_SYNC_TEST=true`, explicit Dart define supplied, release signature verified. Final corrected APK SHA256 `8DA12BE60CB5CD060C0D17272B4C858E1158FE5BC87FEEC6294BAA11CCD70455`, 67,517,126 bytes. Initial corrective installation found USB disconnected. After reconnect, authorized device/checksum/native flag verified, `adb install -r` succeeded and corrected0.9.9+24 launched. No uninstall/storage clearing occurred. Owner private export is next; no phone upload has occurred.

Prepare changes freezes the existing reviewed plan. Private USB export writes the exact first batch UTF-8 bytes to the application's external cache `reviewed-test-batch.json`; native SHA-256 is written to `reviewed-test-batch.metadata.json` with `{version:1,rawUtf8BodySha256,byteLength}`. A compatibility `.sha256` file contains the same lowercase checksum and newline. No device credential, pairing code or certificate private key is exported. The batch contains synthetic service records and must remain in an owner-protected directory outside either repository.

LAN's private driver validates the exact file and checksum, original identity and fresh enrolled device, normal batch schema, and produces canonical hash/operation tuples for review. Preparing a candidate never approves or processes it. A separate explicit confirmation installs the one-batch approval. The laptop receives the file privately by USB; network batch upload happens only when the owner taps Send reviewed test batch.

ConfiguredManualSync checks the current worker, identity, queue, dashboard configuration, stored fingerprint and credential before status/send. It uses the reviewed bytes and one-batch limit. Dashboard admission independently restricts source phone, identity, deadline and exact bytes/tuples. Only exact accepted receipts mark audit operations acknowledged; rejected/missing records remain pending. No retries, retention cleanup or whole-sync completion timestamp are enabled.

Lock, disposal, refresh, reprepare and completion discard exported files. Native activity initialization also removes previous test exports. A stale queue or identity invalidates preparation. The private desktop handoff must be removed after the approved test/closure, following the driver's protected-directory rules. Pairing with a fresh test dashboard preserves phone records/identity; prepare/export again after pairing so the outbound context is current.

Verification: 12 focused export/default UI checks passed, enabled-export exact-body test passed, focused Dart analysis clean. Final signed APK0.9.9+24 was built, signature/version verified, installed with `adb install -r` successfully and launched. APK SHA256 `05364791CB20A652AB18C860C5106E0EEE25C983A935B62DF4D2B7382D52165F`, 66,964,554 bytes. Signing certificate matches the existing release certificate. No uninstall/storage clearing occurred. Owner interaction to prepare/export remains required; no phone upload, listener or firewall change has occurred. Current network checked: laptop192.168.1.7, phone192.168.1.4, USB serialORCE49UWDQVGRC49. Addresses must be checked again before opening the bounded test window.

References: `29_controlled_upload_test_plan.md`, `32_frozen_reviewed_batch.md`, `34_restricted_dashboard_apk_integration.md`, LAN `docs/Outreach_One_Phone_Harness_Preparation.md` and `tools/synthetic_phone_upload/private_driver.ts`.

Private phone bootstrap verified: exact3354-byte copy and matching native checksum/length, normal LAN schema/original identity validation. Metadata shows four revision1 creates: worker1/hotspot1/encounter2; all required parents included for a fresh dashboard. Owner already confirmed all phone pending records synthetic. Copy held in owner-protected temporary handoff outside repositories; no body/credentials copied to documentation. Original device time retains six fractional digits. The preparation helper's HandoffBodyPath flow rejected an auto-converted PowerShell date; LAN notified to fix. Explicit original string parameters successfully prepared enrollment-only certificate/config. Ports3001/3443 free and addresses192.168.1.7/192.168.1.4 rechecked. Await coordinated window readiness; bootstrap body is identity discovery only, not an approved post-pair batch.
